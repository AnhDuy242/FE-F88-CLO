# Work log FE - 2026-07-07

## Bối cảnh

Ngày 2026-07-07, frontend đang được chỉnh theo flow tạo hồ sơ vay nháp 4 màn mới:

1. Màn 1 - Định danh khách hàng.
2. Màn 2 - Thông tin sơ bộ & Gói vay.
3. Màn 3 - Thông tin chi tiết khách hàng và tài sản.
4. Màn 4 - Upload chứng từ & Hoàn tất.

Trong ngày, mục tiêu chính là làm cho FE gọi đúng draft flow mới của BE, đặc biệt ở màn 3 và màn 4, đồng thời loại bỏ các call cũ gây lỗi khi submit hồ sơ.

## Công việc đã làm

### 1. Sửa luồng submit màn 4 theo draft flow mới

Trước đó màn 4 còn đi theo logic cũ:

- Nếu chưa có `applicationCode`, FE cố tạo hồ sơ theo flow cũ.
- FE kiểm tra các trường cũ như `applicationChannel`, `branchCode`, `staffCode`.
- Khi thiếu các trường này, UI báo lỗi:

```text
Thiếu kênh tiếp nhận, mã chi nhánh hoặc mã nhân viên để tạo hồ sơ vay nháp.
```

Logic này không còn phù hợp vì flow mới dùng `loan_application_draft` và `draftCode`.

Đã sửa:

- Màn 4 không còn tự tạo loan application theo flow cũ.
- Màn 4 dùng `draftCode` hiện tại.
- Khi bấm "Gửi đi để phê duyệt", FE gọi:

```http
POST /api/v1/loan-application-drafts/{draftCode}/submit
```

File đã sửa:

- `src/routes/loan/upload-documents.tsx`
- `src/features/loan-onboarding/api/loan-application-draft.api.ts`
- `src/constants/api-endpoints.ts`

### 2. Thêm API submit draft vào FE

Đã bổ sung endpoint:

```ts
submit: (draftCode: string) => `/loan-application-drafts/${draftCode}/submit`
```

Đã bổ sung hàm API:

```ts
loanApplicationDraftApi.submit(draftCode, payload)
```

Payload gửi lên BE:

```ts
{
  documents: [
    {
      documentTypeCode: string,
      fileUrl: string,
      fileName: string
    }
  ]
}
```

Hiện tại FE chưa có API upload S3 thật. Vì vậy `fileUrl` đang dùng URL preview/local của file trên UI. Khi có API upload S3 chính thức, đoạn này cần đổi sang URL S3 trả về.

### 3. Chuẩn hóa mã chứng từ gửi từ FE sang BE

Trước đó FE dùng mã theo UI như:

- `cccd-front`
- `cccd-back`
- `asset-front`
- `asset-back`
- `portrait-video`

Trong khi BE kiểm tra mã theo dạng nghiệp vụ:

- `CITIZEN_ID_FRONT`
- `CITIZEN_ID_BACK`
- `ASSET_FRONT`
- `ASSET_REAR`
- `CUSTOMER_PORTRAIT_VIDEO`

Đã thêm mapping ở màn 4:

```ts
const SUBMIT_DOCUMENT_CODE_BY_SLOT_ID = {
  "cccd-front": "CITIZEN_ID_FRONT",
  "cccd-back": "CITIZEN_ID_BACK",
  "vehicle-registration-front": "VEHICLE_REGISTRATION_FRONT",
  "vehicle-registration-back": "VEHICLE_REGISTRATION_BACK",
  "asset-front": "ASSET_FRONT",
  "asset-back": "ASSET_REAR",
  "asset-left": "ASSET_LEFT",
  "asset-right": "ASSET_RIGHT",
  "frame-number": "ASSET_FRAME_NUMBER",
  "engine-number": "ASSET_ENGINE_NUMBER",
  "odo": "ASSET_ODO",
  "portrait": "CUSTOMER_PORTRAIT",
  "portrait-with-cccd": "BORROWER_HOLDING_CITIZEN_ID_IMAGE",
  "portrait-video": "CUSTOMER_PORTRAIT_VIDEO"
}
```

Mục đích:

- FE vẫn giữ code dễ hiểu theo UI.
- Payload submit gửi lên BE đúng mã mà backend đang validate.

### 4. Sửa màn 3 để hoàn tất đúng bước `CUSTOMER_ASSET_LOAN_PROPOSAL`

Khi submit màn 4, BE báo:

```text
Bản nháp có các bước chưa đầy đủ: CUSTOMER_ASSET_LOAN_PROPOSAL
```

Nguyên nhân:

- Màn 3 trước đó chỉ gọi save step với trạng thái `IN_PROGRESS`.
- Khi nhân viên bấm "Tiếp tục" sang màn 4, bước 3 vẫn chưa được đánh dấu `COMPLETED`.

Đã sửa:

- Trong lúc nhập màn 3, autosave/lưu nháp vẫn là `IN_PROGRESS`.
- Khi bấm "Tiếp tục" ở màn 3, FE gọi API complete step:

```http
POST /api/v1/loan-application-drafts/{draftCode}/steps/CUSTOMER_ASSET_LOAN_PROPOSAL/complete
```

Đã thêm hàm:

```ts
loanApplicationDraftApi.completeCustomerAssetLoanProposal(...)
```

File đã sửa:

- `src/features/loan-onboarding/api/loan-application-draft.api.ts`
- `src/routes/loan/customer-asset-detail.tsx`

### 5. Loại bỏ autosave PUT ở màn 4

Sau khi submit thành công, UI hiển thị:

```text
Draft converted to loan application
```

Nhưng F12 vẫn báo lỗi lặp:

```http
PUT /api/v1/loan-application-drafts/{draftCode}/steps/UPLOAD_COMPLETE 400
```

Message:

```text
Draft is not editable in status CONVERTED
```

Nguyên nhân:

- Submit đã thành công.
- Draft đã chuyển sang `CONVERTED`.
- Nhưng màn 4 vẫn còn autosave gọi `PUT /steps/UPLOAD_COMPLETE`.
- BE từ chối vì draft đã convert thì không được chỉnh sửa nữa.

Đã sửa:

- Bỏ hoàn toàn `useDraftStepAutosave` khỏi màn 4.
- Bỏ trạng thái "Đang lưu nháp / Đã lưu nháp" ở màn 4.
- Màn 4 chỉ còn call POST submit khi bấm nút "Gửi đi để phê duyệt".

Sau sửa, màn 4 không còn gọi:

```http
PUT /api/v1/loan-application-drafts/{draftCode}/steps/UPLOAD_COMPLETE
```

File đã sửa:

- `src/routes/loan/upload-documents.tsx`

### 6. Sửa hook autosave để không báo đỏ ESLint

File:

- `src/features/loan-onboarding/hooks/use-draft-step-autosave.ts`

Sau khi tắt autosave, file hook bị ESLint báo lỗi unused vars:

```text
'_draftCode' is defined but never used
'_stepCode' is defined but never used
'_data' is defined but never used
```

Đã sửa bằng cách giữ hook là no-op và dùng `void` cho các tham số:

```ts
void draftCode;
void stepCode;
void data;
void enabled;
void debounceMs;
void skipInitialSave;
```

Kết quả:

- Hook không còn gọi PUT.
- ESLint riêng file hook pass.
- Build FE pass.

### 7. Thêm API tính scoring cho màn 3

Trước đó sidebar màn 3 hiển thị:

```text
Chưa có dữ liệu scoring từ backend.
```

Nguyên nhân:

- FE chưa gọi API credit scoring.

Đã thêm file API:

- `src/features/customer-asset-detail/api/credit-scoring.api.ts`

Đã thêm endpoint:

```ts
creditScoring: {
  calculate: "/credit-scoring/calculate"
}
```

Mục tiêu:

- FE có thể gọi:

```http
POST /api/v1/credit-scoring/calculate
```

- Dữ liệu dùng để hiển thị phần scoring ở sidebar màn 3.

### 8. Liên kết GitHub cho FE

Đã liên kết FE với GitHub:

Repository:

```text
https://github.com/AnhDuy242/FE-F88-CLO.git
```

Branch:

```text
tuanfe
```

Thư mục repo local đúng:

```text
D:\F88\FE\FE-F88-CLO-main
```

Lưu ý:

- GitHub repo có cấu trúc root chứa thư mục `customer_loan_ob`.
- Ban đầu nếu init Git ở trong `customer_loan_ob` thì sai cấp thư mục.
- Đã xóa `.git` nhầm trong `customer_loan_ob` và init lại ở root `FE-F88-CLO-main`.

Trạng thái sau liên kết:

```text
branch tuanfe tracks origin/tuanfe
```

## File FE đã thay đổi chính

- `src/routes/loan/upload-documents.tsx`
- `src/routes/loan/customer-asset-detail.tsx`
- `src/features/loan-onboarding/api/loan-application-draft.api.ts`
- `src/features/loan-onboarding/hooks/use-draft-step-autosave.ts`
- `src/constants/api-endpoints.ts`
- `src/features/customer-asset-detail/api/credit-scoring.api.ts`

## Kiểm thử đã chạy

Đã chạy:

```bash
npm run build
```

Kết quả:

```text
build pass
```

Đã chạy ESLint riêng file autosave:

```bash
npx eslint src/features/loan-onboarding/hooks/use-draft-step-autosave.ts
```

Kết quả:

```text
pass
```

Đã kiểm tra FE dev server:

```text
http://localhost:5173 -> 200
```

## Điểm còn lưu ý

### 1. Màn 4 chưa upload S3 thật

Hiện tại màn 4 vẫn chỉ chọn file ở browser và gửi `fileUrl` tạm từ preview/local URL khi submit.

Khi có API upload S3 chính thức, cần đổi flow thành:

1. FE upload file lên S3 hoặc BE upload API.
2. Nhận URL thật.
3. Khi submit, gửi URL thật vào `documents[]`.

### 2. Hook autosave đang bị no-op

Hiện tại `useDraftStepAutosave` đã được tắt để tránh PUT gây lỗi với flow mới.

Nếu muốn dùng autosave lại cho màn 1, 2, 3 thì cần thiết kế lại rõ:

- Màn nào được autosave bằng PUT.
- Màn nào chỉ được complete bằng POST.
- Sau khi draft `CONVERTED` thì tất cả autosave phải dừng.

### 3. ESLint toàn project vẫn còn lỗi cũ

`npm run lint` toàn project vẫn còn nhiều lỗi không thuộc phần sửa hôm nay, ví dụ:

- `react-refresh/only-export-components`
- `react-hooks/set-state-in-effect`
- `react-hooks/purity`

Riêng file autosave đã được xử lý pass.

